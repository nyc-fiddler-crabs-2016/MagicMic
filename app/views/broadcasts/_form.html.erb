<%= form_for @broadcast do |f| %>

	<%= f.label :topic %>
	<%= f.text_field :topic %>
	<%= f.label :datetime %>
	<%= f.text_field :datetime %>
	<%= f.label :duration %>
	<%= f.text_field :duration %>
	<%= f.submit :submit %>

<%= end %>